// Veamos como acceder a los elementos de un array...

const numeros = [10, 20, 30, 40, 50];

// esta vez estare utilizando
console.table(numeros);

// La forma en la que accedes a un array es por lo que se conoce como el indice en el array,
// los arrays inician en 0 usualmente aunuque hay lenguajes donde inician en 1

console.log(numeros[0]);
console.log(numeros[1]);
console.log(numeros[3]);
console.log(numeros[20]);
