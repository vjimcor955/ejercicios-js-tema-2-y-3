// Veamos la diferencia entre un método y una función...
const numero1 = 20;
const numero2 = "20";

console.log(parseInt(numero2)); // Función

console.log(numero1.toString()); // Método

// La función toma el valor en el parentesis.
// El método añade un punto seguido del nombre.
